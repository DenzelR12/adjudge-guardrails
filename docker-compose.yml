services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: adjudge
      POSTGRES_PASSWORD: adjudge
      POSTGRES_DB: adjudge_demo
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
